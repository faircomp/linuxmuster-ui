## p7-fe-toolchain-catchup [P7] — Frontend- & Toolchain-Nachzug auf 2.1.0 (React 19, ESLint 9, 4 Contract-Änderungen, Baselines)
_Ziel:_ Fork-FE + Toolchain auf den 2.1.0-Stand ziehen; die vier harten Contract-Brüche schließen; Bundle-Diff + neue Baselines · _Abhängt-von:_ (T5: organizationType-Migration 007 aus dem GlobalSettings-Paket) · _Status:_ **offen (0/24)** · _Tasks:_ 24
Branch: `feat/2.0-backlog` · Spec: `docs/features/p7-fe-toolchain-catchup.md` · Soll (API-Bundle, un-minifiziert): `.reference/2.1.0/api/main.js` — APPS NEW:188-231 · ExtendedOptionKeys NEW:2363-2417 · ThemeColors-Schema NEW:5905-5931 · DEFAULT_THEME NEW:5975-5990 · GlobalSettings-Migrationsliste NEW:8267-8287 · migration007 NEW:8764-8787 · migration008 NEW:8817-8853 · getMailDomains NEW:27881-27883 + Decorators NEW:28387-28394 + MAIL_ENDPOINT_PATHS.DOMAINS NEW:28917 · multer-Utilities NEW:65960-66000 · TldrawSyncRoom-Schema NEW:74318-74338 · TLDrawSyncService.onModuleInit NEW:74411-74413 / assertRoomAccess NEW:74414-74429 · tldrawSyncRoomMigrationsList NEW:74703-74706 · migration000NamespaceAssetsByRoom NEW:74742-74806 · namespaceLegacyTldrawAssetUrl NEW:74836-74855 · TLDRAW_SYNC_ENDPOINTS NEW:74882-74889 · TLDrawSyncController NEW:75238-75344 · WhiteboardAssetAccessGuard NEW:75404-75435 · isValidTldrawRoomId NEW:75466-75472 · TLDRAW_ROOM_ID_SEGMENT_PATTERN NEW:75499 · FE-Bundle (nur minifiziert, Struktur-Evidenz): `.reference/2.1.0/ui/assets/TLDrawWithSync-91KtZc9A.js`, `index-CHsBOwUD.js`

> **Kontext-Notiz (gilt für JEDE Task dieses Pakets):** Der 2.1.0-**Frontend-Quellcode ist nicht verfügbar**.
> Un-minifiziert existiert nur `.reference/2.1.0/api/main.js`. Das FE liegt ausschließlich als **gebautes,
> minifiziertes Bundle** vor (`.reference/2.1.0/ui/`). Jede FE-Komponenten-Task hier ist deshalb
> **fork-originales Design, informiert vom API-Contract** — ausdrücklich KEINE Rekonstruktion. Wo das
> minifizierte Chunk die Struktur belegt (T11), ist das benannt; Bezeichner bleiben trotzdem fork-original.
> SPDX neuer Dateien: `SPDX-License-Identifier: AGPL-3.0-or-later` + `Copyright (C) 2026 Kevin Stenzel`
> (nicht der Netzint-Dual-Header). Markdown-Doku ohne Header. Bestandsdateien behalten ihren Header.

---

### T1 — fetch-reference.sh auf 2.1.0 als Default umstellen  [ ]
Komponente: apps/api (Tooling) · Dateien: `scripts/crabbox/fetch-reference.sh`
Soll: Kein Bundle-Anker (Skript-Änderung). Fakten: `.reference/2.1.0/PROVENANCE.txt` = `2.1.0 (rev 5c58d5818c1efff0422e914bf5785209b972cec6, main.js=94907 Zeilen)`; `.reference/2.0.200/PROVENANCE.txt` = `2.0.200 (rev 7356c68)`.
Änderung: `EXPECT_VER="${REF_VERSION:-2.0.200}"` → `"${REF_VERSION:-2.1.0}"` und `EXPECT_LINES="${EXPECT_LINES-73240}"` → `"${EXPECT_LINES-94907}"`. Den Kopfkommentar (Zeilen 6-11) so umschreiben, dass 2.1.0 die Default-Rekonstruktionsbasis ist und 2.0.200 per `REF_VERSION=2.0.200 EXPECT_LINES=73240 …` weiter holbar bleibt. Sonst nichts anfassen (Layer-Logik, Versions-Pin, PROVENANCE-Schreiben bleiben unverändert).
Verify: `bash -n scripts/crabbox/fetch-reference.sh` (rc=0) und `grep -q 'REF_VERSION:-2.1.0' scripts/crabbox/fetch-reference.sh && grep -q 'EXPECT_LINES-94907' scripts/crabbox/fetch-reference.sh && echo OK` → `OK`. Kein Netz-Aufruf im Verify (die Referenzen liegen bereits lokal).
i18n: keine
Doku: keine (Kopfkommentar im Skript selbst)
Abhängt von: —

### T2 — Bundle-Fakten-Extraktor (acorn) für gebaute UI-Bundles  [ ]
Komponente: apps/frontend (Tooling) · Dateien: `scripts/reference/extractBundleFacts.mjs` (neu)
Soll: Kein un-minifizierter Anker — das FE-Bundle ist minifiziert. Das Skript ist fork-originales Werkzeug. Verifizierte Machbarkeit: `acorn@8.15.0` liegt bereits als Prod-Dependency im Baum; ein Parse von `index-CHsBOwUD.js` (5,7 MB) mit `--max-old-space-size=6000` läuft durch und liefert 3 i18n-Ressourcen-Objekte (de/en/fr) mit 164/164/165 Top-Level-Keys.
Änderung: Node-ESM-Skript mit SPDX-`//`-Header. CLI: `node scripts/reference/extractBundleFacts.mjs <ui-assets-dir> <out.json>`. Es sammelt aus dem Verzeichnis:
(a) **React-Major** — Sentinel-Grep über alle `*.js`: `__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED` = 18, `__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE` = 19.
(b) **react-helmet-async vorhanden?** — Marker `data-rh`, `helmetData`, `helmetInstances` (die Treffer `helmet-safety`/`helmet-un` sind Emoji-Namen und müssen ausgeschlossen werden).
(c) **tldraw-Versionen** — Regex ``/`(@tldraw\/[a-z-]+|tldraw)`,`(\d+\.\d+\.\d+)`/g``.
(d) **Tailwind-CI-Farbklassen** — Zählung von `text-|bg-|border-` × `ciLightBlue|ciDarkBlue` über `*.js` **und** `*.css`.
(e) **i18n-Top-Level-Namespaces** — `acorn.parse(src,{ecmaVersion:'latest',sourceType:'module'})` über die `index-*.js`, Walk über alle `ObjectExpression` mit >15 Properties, Treffer = Objekte, deren Key-Menge sowohl `settings` als auch `common` enthält; Rückgabe die Vereinigung der Key-Mengen, sortiert.
(f) **Chunk-Namen** — Basenames aller `*.js` ohne den 8-Zeichen-Hash-Suffix (`/-[A-Za-z0-9_-]{8}\.js$/`), dedupliziert.
Keine Netz-Zugriffe, keine Schreibvorgänge außerhalb von `<out.json>`. `scripts/**` steht in `ignorePatterns` der ESLint-Config → wird nicht gelintet; `.mjs` wird von `addLicenseHeader` nicht gestempelt, der SPDX-Header ist deshalb von Hand zu setzen.
Verify: lokal (kein Box-Bedarf, `.reference/` ist gitignored und nur lokal vorhanden):
`node --max-old-space-size=6000 scripts/reference/extractBundleFacts.mjs .reference/2.1.0/ui/assets /tmp/f210.json && node --max-old-space-size=6000 scripts/reference/extractBundleFacts.mjs .reference/2.0.200/ui/assets /tmp/f200.json`
Erwartet (harte Assertions, alle vorab gemessen): `f210.react === 19`, `f200.react === 18`; `f210.helmet === false`, `f200.helmet === true`; `f210.tldraw['@tldraw/sync'] === '3.15.6'`, `f200.tldraw['@tldraw/sync'] === '3.15.4'`; `f210.ciClasses['text-ciDarkBlue'] === 12 && f210.ciClasses['text-ciLightBlue'] === 0`, umgekehrt bei 2.0.200 (`text-ciLightBlue` = 8); `f210.i18nNamespaces.length === 165`, `f200.i18nNamespaces.length === 157`.
i18n: keine
Doku: keine (T3 nutzt die Ausgabe)
Abhängt von: —

### T3 — docs/analysis: Bundle-Diff 2.0.200 ↔ 2.1.0 ↔ Fork  [ ]
Komponente: apps/frontend (Doku) · Dateien: `docs/analysis/ui-bundle-diff-2.0.200-vs-2.1.0.md` (neu)
Soll: Ausgabe von T2 + API-Bundle-Anker: APPS NEW:188-231 (2.0.200-Pendant: `.reference/2.0.200/api/main.js:188`), ExtendedOptionKeys NEW:2363-2417 (2.0.200: Zeile 2075). Das FE ist nur als Bundle greifbar — der Report muss das in einem eigenen Abschnitt „Beweiskraft/Grenzen" explizit sagen.
Änderung: Report mit den bereits gemessenen Befunden (alle Zahlen sind verifiziert, der ausführende Agent reproduziert sie mit T2 und den unten genannten Kommandos, statt sie abzuschreiben):
1. **Toolchain:** React 18 → 19; `react-helmet-async` entfernt; tldraw 3.15.4 → 3.15.6; Bundler bleibt rolldown (`rolldown-runtime-*.js` in beiden); `index.html`-Viewport verliert `maximum-scale=1.0`.
2. **Chunks:** exakt 2 neue (`mermaid-GHXKKRXX`, `highlighted-body-OFNGDK62`), 0 entfallene.
3. **Tailwind/CI-Farben:** `text|bg|border-ciLightBlue` (8/6/6) → `…-ciDarkBlue` (12/6/6); i18n-Keys `settings.globalSettings.theme.ciLightBlue{,Description}` → `ciDarkBlue{,Description}` (im Bundle als „Markenblau"/„Brand Blue"/„Bleu de marque" nachweisbar).
4. **i18n-Namespaces:** 157 → 165. Neu in 2.1.0: `contacts`, `emojiPicker`, `examuser`, `maillist`, `parentClassGroup`, `parentGroup`, `shareToApp`, `wysiwygEditor`; entfallen: keine. Gegenüber dem Fork fehlen 21: die 8 obigen plus `content`, `deviceManagement`, `guacamole`, `landingPage`, `linbo`, `markdownEditor`, `nativeApp`, `organizationType`, `parentAssignment`, `satellites`, `shiftKey`, `ssh`, `ssh-terminal`; im Fork zusätzlich vorhanden: `mailcowAdmin`.
5. **APPS-Slugs** (aus dem API-Bundle, autoritativ): 2.1.0 hat 41 Einträge, 2.0.200 hat 40 (`CONTACTS: 'contacts'` neu). Dem Fork (`libs/src/appconfig/constants/apps.ts`, 39 Einträge) fehlen **`CONTACTS`** und **`SATELLITES`**.
6. **ExtendedOptionKeys:** 2.1.0 hat 52, der Fork 36 — 20 fehlen (`CONTACTS_CARDDAV_{AUTH_MODE,BASE_URL,REJECT_UNAUTHORIZED}`, `EURO_OFFICE_{JWT_SECRET,URL}`, `IN_APP_PERMISSION_{AI_CHAT,CREATE,PARTICIPATE}`, `MAIL_{IMAP_HOST,MAILBOX_TABLE,MANAGESIEVE_HOST,MANAGESIEVE_PORT,PROVIDER_CONFIG_TABLE,SIGNATURE,SMTP_HOST,SMTP_PORT,TLS_REJECT_UNAUTHORIZED}`, `MOBILE_DEVICES_{ALLOW_ADOPT_EXISTING,SYNCED_GROUPS}`, `WIKI_SHARE_VISIBILITY_TABLE`).
7. **Routen:** API-Controller-Basen 39 → 41 (`grep -c 'common_1.Controller)(' main.js` je Bundle). FE-Routen sind **nicht** direkt extrahierbar (Pfade werden aus `APPS`-Konstanten zusammengesetzt, im Bundle keine literalen `/pfad`-Strings) — das ist als Grenze zu notieren, die APPS-Slugs sind der Ersatz-Maßstab.
8. **Befunde außerhalb dieses Pakets** (nur festhalten, nicht bauen): Whiteboard-Upload komprimiert in 2.0.200/2.1.0 vor dem POST, im Fork nicht; `DropdownSelect` hat in 2.1.0 ein `renderLabel`-Prop; die Punkte 5/6 gehören in die Modul-Pakete.
Jede Zeile mit ihrem Reproduktions-Kommando versehen (T2-Skript bzw. `grep`/`sed` auf `main.js` mit Zeilennummer).
Verify: `iter.sh cmd 'test -f docs/analysis/ui-bundle-diff-2.0.200-vs-2.1.0.md && grep -q "__CLIENT_INTERNALS" docs/analysis/ui-bundle-diff-2.0.200-vs-2.1.0.md && grep -q "3.15.6" docs/analysis/ui-bundle-diff-2.0.200-vs-2.1.0.md && grep -q "CONTACTS" docs/analysis/ui-bundle-diff-2.0.200-vs-2.1.0.md && echo OK'` → `OK`. Zusätzlich lokal: die 8 neuen i18n-Namespaces und die 20 fehlenden ExtendedOptionKeys sind vollständig im Dokument gelistet (`grep -c` = 8 bzw. 20).
i18n: keine
Doku: dieses Dokument selbst; Querverweis aus `PLAN-openedulution-fork.md` auf den Report setzen
Abhängt von: T2

### T4 — ciLightBlue → ciDarkBlue: atomarer Rename über libs + API-Schema + FE + Tailwind + i18n  [ ]
Komponente: libs, apps/api, apps/frontend · Dateien: `libs/src/global-settings/types/themeColors.ts` (Z. 24), `libs/src/global-settings/constants/defaultTheme.ts` (Z. 25, 31), `apps/api/src/global-settings/schemas/global-settings.theme.schema.ts` (Z. 35-36), `apps/frontend/tailwind.config.ts` (Z. 22), `apps/frontend/src/utils/applyThemeColors.ts` (Z. 26), `apps/frontend/src/pages/Settings/components/ThemeSettings.tsx` (Z. 77), `apps/frontend/src/pages/FileSharing/Dialog/DialogBodys/WebdavInfoDialogBody.tsx` (Z. 81, 118), `apps/frontend/src/pages/FileSharing/utilities/ShareLinkScopeSelector.tsx` (Z. 52), `apps/frontend/src/components/ui/Loading/CircleLoader.tsx` (Z. 43), `apps/frontend/src/components/shared/Card/Card.tsx` (Z. 29), `apps/frontend/src/components/structure/framing/ResizableWindow/Buttons/SaveButton.tsx` (Z. 38), `apps/frontend/src/locales/{de,en,fr}/translation.json`
Soll: NEW:5905-5931 (`ThemeColors` mit Feld `ciDarkBlue`, `@Prop({type:String, default: DEFAULT_THEME.light.ciDarkBlue})` auf NEW:5925-5927) · NEW:5975-5990 (`DEFAULT_THEME`, `ciDarkBlue: '#0081C6'` auf NEW:5980 und NEW:5986 — beachte die **Großschreibung** `#0081C6`, der Fork hat heute `#0081c6`). Für die Tailwind-Klassen und die i18n-Labels liefert nur das **gebaute** 2.1.0-Bundle Evidenz (kein FE-Quellcode): `text-ciDarkBlue`=12 / `bg-ciDarkBlue`=6 / `border-ciDarkBlue`=6 gegen 0 Treffer der `ciLightBlue`-Varianten, und die Strings `"ciDarkBlue":"Markenblau"` / `"ciDarkBlueDescription"` im `index-CHsBOwUD.js`. Die konkreten Komponenten, in denen die Klassen stehen, sind **fork-original** — das Bundle beweist nur, dass upstream den Farbschlüssel mit umbenannt hat.
Änderung: **Ein Commit, atomar** (Contract-Sync; die Surgical-Regel gilt hier nicht — jeder Teilschritt allein lässt `tsc` rot):
- `themeColors.ts`: Feld `ciLightBlue: string` → `ciDarkBlue: string`.
- `defaultTheme.ts`: beide `ciLightBlue: '#0081c6'` → `ciDarkBlue: '#0081C6'`.
- `global-settings.theme.schema.ts`: `@Prop({ type: String, default: DEFAULT_THEME.light.ciDarkBlue }) ciDarkBlue: string;`.
- `tailwind.config.ts`: Farbschlüssel `ciLightBlue: 'var(--ci-dark-blue)'` → `ciDarkBlue: 'var(--ci-dark-blue)'`. Die CSS-Variable `--ci-dark-blue` heißt **bereits** so und bleibt unverändert (`index.scss:19`, `:100`, `surveyTheme.ts:56-58`, alle SVG-Assets, `HexagonIcon.tsx:55`, `tailwind.config.ts:46` `ciGreenToBlue`) — dort nichts anfassen.
- `applyThemeColors.ts:26`: `root.style.setProperty('--ci-dark-blue', theme.ciDarkBlue)`.
- `ThemeSettings.tsx:77`: `{ key: 'ciDarkBlue', labelKey: 'ciDarkBlue', descriptionKey: 'ciDarkBlueDescription' }`.
- Die 6 Klassen-Vorkommen: `text-ciLightBlue` → `text-ciDarkBlue` (WebdavInfoDialogBody ×2, ShareLinkScopeSelector), `border-t-ciLightBlue` → `border-t-ciDarkBlue` (CircleLoader), `border-ciLightBlue` → `border-ciDarkBlue` (Card), `bg-ciLightBlue` → `bg-ciDarkBlue` (SaveButton).
- i18n in allen drei Locales unter `settings.globalSettings.theme`: `ciLightBlue`→`ciDarkBlue`, `ciLightBlueDescription`→`ciDarkBlueDescription`; Werte unverändert (DE „Markenblau" / „Blaue Markenfarbe für Farbverläufe und Logos", EN „Brand Blue" / „Brand blue color used in gradients and logos", FR „Bleu de marque" / „Couleur bleue de marque utilisée dans les dégradés et les logos").
Nicht anfassen: `libs/src/global-settings/constants/defaultValues.ts` (spreadet `DEFAULT_THEME`), `global-settings.service.ts:167/171` (spreadet ebenfalls), `getThemeWithDefaults.ts`. Es gibt **keine** class-validator-DTO mit Farbfeldern (geprüft).
Verify: `iter.sh cmd 'grep -rn "ciLightBlue" apps libs --include="*.ts" --include="*.tsx" --include="*.json" | grep -v node_modules'` → **leere Ausgabe**. Dann `iter.sh lint`, `iter.sh i18n` (`check-translations` erzwingt DE/EN/FR-Parität), `iter.sh test:api`, `iter.sh test:frontend` und `iter.sh cmd 'npx tsc -p apps/frontend/tsconfig.app.json --noEmit'` alle grün (isolierter tsc ist Pflicht — eslint+vitest verdecken fehlende/umbenannte Props). Zusätzlich `iter.sh cmd 'npm run build'` grün und `grep -c "ciDarkBlue" dist/apps/frontend/assets/*.css` > 0.
i18n: `settings.globalSettings.theme.ciDarkBlue` und `.ciDarkBlueDescription` in DE+EN+FR (die alten `ciLightBlue*`-Keys entfallen in allen drei)
Doku: keine (Farbschlüssel ist intern); in `docs/analysis/ui-bundle-diff-2.0.200-vs-2.1.0.md` als „umgesetzt" markieren
Abhängt von: —

### T5 — GlobalSettings-Migration 008: ciLightBlue → ciDarkBlue (schemaVersion 9 → 10)  [ ]
Komponente: apps/api · Dateien: `apps/api/src/global-settings/migrations/migration008.ts` (neu), `apps/api/src/global-settings/migrations/globalSettingsMigrationsList.ts`, `apps/api/src/global-settings/migrations/migration008.spec.ts` (neu)
Soll: NEW:8817-8853 — `OLD_CI_LIGHT_BLUE_DEFAULT = '#67b2e0'` (NEW:8817), `NEW_CI_DARK_BLUE_DEFAULT = DEFAULT_THEME.light.ciDarkBlue` (NEW:8818), `resolveCiDarkBlue` (NEW:8819-8824), `migration008` mit `name: '008-rename-ciLightBlue-to-ciDarkBlue'`, `version: 8`, `previousSchemaVersion = 9`, `newSchemaVersion = 10` (NEW:8825-8853). Listen-Einbindung NEW:8267-8287.
Änderung: **Vorbedingung zuerst prüfen** — enthält `globalSettingsMigrationsList.ts` bereits ein `migration007` (`007-add-organizationType`, 8→9, SOLL NEW:8764-8787)? Wenn **nein**: Task auf `[?] human-gate` setzen, kurz vermerken, weiterlaufen — **nicht umnummerieren** (sonst bricht die Monotonie, sobald das organizationType-Paket landet). Wenn **ja**: neue Datei `migration008.ts` mit AGPL-SPDX-Header, `Migration<GlobalSettingsDocument>` nach dem Muster von `migration006.ts` (`/* eslint-disable no-underscore-dangle */`, `Logger.debug(..., migration008.name)` bei fehlendem Dokument, statischer NestJS-Logger). Ablauf 1:1 wie SOLL: `findOne({ schemaVersion: 9 }).lean()`; bei Treffer `updateOne({_id}, { $set: { 'theme.light.ciDarkBlue': resolveCiDarkBlue(gs.theme?.light?.ciLightBlue), 'theme.dark.ciDarkBlue': resolveCiDarkBlue(gs.theme?.dark?.ciLightBlue), schemaVersion: 10 }, $unset: { 'theme.light.ciLightBlue': '', 'theme.dark.ciLightBlue': '' } })`; Abschluss-Log `Migration 008 complete: ${result.modifiedCount} document updated.`. `resolveCiDarkBlue(legacy)` gibt `DEFAULT_THEME.light.ciDarkBlue` zurück, wenn `legacy` falsy ist **oder** `legacy.toLowerCase() === '#67b2e0'`, sonst `legacy` unverändert (individuell gesetzte Farben überleben). Konstanten als `const` (keine magic strings im Code). In die Liste am Ende eintragen (Reihenfolge ist die Ausführungsreihenfolge). Jest-Spec: (a) kein Dokument mit `schemaVersion: 9` → `updateOne` nicht aufgerufen; (b) Legacy-Default `'#67B2E0'` (Groß/Klein gemischt) → beide `ciDarkBlue` = `DEFAULT_THEME.light.ciDarkBlue`; (c) individueller Wert `'#ff0000'` → bleibt `'#ff0000'`; (d) fehlendes `theme.dark.ciLightBlue` (undefined) → Default; (e) `$unset` enthält beide Legacy-Pfade und `$set.schemaVersion === 10`.
Verify: `iter.sh test:api` grün inkl. `migration008.spec.ts` mit den fünf Assertions oben; `iter.sh cmd 'npm run build:api'` grün; `iter.sh cmd "grep -c 'migration00' apps/api/src/global-settings/migrations/globalSettingsMigrationsList.ts"` zeigt die vollständige Kette 000–008.
i18n: keine
Doku: Eintrag in `docs/migrations/` nach dem dort etablierten Muster (Modell `GlobalSettings`, 9→10, forward-only, Rollback = Dump **+ `./data/master.key`**)
Abhängt von: T4 · (paket-übergreifend) migration007 `007-add-organizationType` aus dem GlobalSettings/organizationType-Paket

### T6 — libs: tldraw-Room-ID-Pattern, isValidTldrawRoomId, namespaceLegacyTldrawAssetUrl  [ ]
Komponente: libs, apps/frontend (Test-Config) · Dateien: `libs/src/tldraw-sync/constants/tldrawRoomIdSegmentPattern.ts` (neu), `libs/src/tldraw-sync/utils/isValidTldrawRoomId.ts` (neu), `libs/src/tldraw-sync/utils/namespaceLegacyTldrawAssetUrl.ts` (neu), `libs/src/tldraw-sync/utils/isValidTldrawRoomId.spec.ts` (neu), `libs/src/tldraw-sync/utils/namespaceLegacyTldrawAssetUrl.spec.ts` (neu), **`apps/frontend/vite.config.mts` (Z. 59)**
**BLOCKER, der zuerst zu lösen ist — sonst ist der Verify falsch-grün:** `libs/**`-Specs werden **von keiner Suite ausgeführt**. `apps/frontend/vite.config.mts` hat `root: __dirname` (Z. 68) und `test.include: ['src/**/*.{test,spec}.…']` (Z. 59) → nur `apps/frontend/src`. `apps/api/jest.config.ts` erbt aus `jest.preset.cjs` das `testMatch` mit rootDir `apps/api` → ebenfalls nicht `libs`. Im ganzen Fork existiert **keine einzige** `libs/**/*.spec.*` (`find libs -name '*.spec.*'` = 0). Ohne Fix laufen die vier neuen Specs aus T6+T11 nie und `iter.sh test:frontend` meldet trotzdem grün.
Fix (Teil dieser Task, minimal): in `apps/frontend/vite.config.mts` das `test.include`-Array um `'../../libs/src/**/*.{test,spec}.{ts,tsx}'` erweitern. Der Alias `@libs → ../../libs/src` ist dort bereits gesetzt, `environment: 'jsdom'` schadet den reinen Utils nicht. Sonst nichts an der Config anfassen.
Soll: NEW:75499 `TLDRAW_ROOM_ID_SEGMENT_PATTERN = /^[a-zA-Z0-9._-]+$/` · NEW:75466-75472 `isValidTldrawRoomId(roomId)` = Pattern-Treffer **und** Prefix `multi-` oder `single-` · NEW:74836-74855 `namespaceLegacyTldrawAssetUrl(url, roomId)` mit `ASSET_URL_MARKER = '/tldraw-sync/assets/'`.
Änderung: Drei neue Dateien mit AGPL-SPDX-Header, Default-Export am Dateiende, Dateiname == Export-Name, keine Kommentare im Code.
- `tldrawRoomIdSegmentPattern.ts`: `const TLDRAW_ROOM_ID_SEGMENT_PATTERN = /^[a-zA-Z0-9._-]+$/;` (das Pattern schließt `/` und damit Traversal im Pfadsegment aus — das ist der Grund, warum der Guard vor dem multer-Interceptor greifen muss, siehe T9).
- `isValidTldrawRoomId.ts`: `(roomId: string) => boolean`; `false` bei leerem Wert oder Pattern-Miss; sonst `roomId.startsWith(TLDRAW_MULTI_USER_ROOM_PREFIX) || roomId.startsWith(TLDRAW_SINGLE_USER_ROOM_PREFIX)` (bestehende Konstanten aus `@libs/whiteboard/constants/tldraw{Multi,Single}UserRoomPrefix`, Werte `'multi-'`/`'single-'`).
- `namespaceLegacyTldrawAssetUrl.ts`: `(url: unknown, roomId: string) => { newUrl: string; encodedFilename: string } | null`. `null` bei Nicht-String, fehlendem Marker, leerem Rest oder wenn der Rest bereits ein `/` enthält (= schon namespaced). Sonst `{ newUrl: prefix + encodeURIComponent(roomId) + '/' + rest, encodedFilename: rest }`, wobei `prefix` alles bis inklusive Marker ist. `ASSET_URL_MARKER` aus `TLDRAW_SYNC_ENDPOINTS.BASE`/`.ASSETS` zusammensetzen (keine magic strings).
Specs (vitest, `libs` läuft unter der Frontend-Suite): `isValidTldrawRoomId` → `'multi-7a'` true, `'single-alice'` true, `'7a'` false (kein Prefix), `'multi-a/b'` false, `'multi-../x'` false, `''` false. `namespaceLegacyTldrawAssetUrl` → `('https://h/edu-api/tldraw-sync/assets/alice_a1.png','multi-7a')` ergibt `newUrl` mit `/assets/multi-7a/alice_a1.png` und `encodedFilename==='alice_a1.png'`; bereits namespacete URL → `null`; URL ohne Marker → `null`; Nicht-String → `null`.
Verify:
- `iter.sh test:frontend` grün **und** die neuen Specs sind nachweislich gelaufen: `iter.sh cmd 'npx nx test frontend --run 2>&1 | tee /tmp/vt.log; grep -q isValidTldrawRoomId /tmp/vt.log && grep -q namespaceLegacyTldrawAssetUrl /tmp/vt.log && echo SPECS-RAN'` → `SPECS-RAN` (ohne diese Prüfung ist der Verify wertlos, siehe Blocker oben)
- `iter.sh lint` sauber
- Dateinamen-Check **explizit** statt über `npm run check-filenames` (das ist `git diff --name-only --cached | xargs tsx scripts/checkFilenames.ts` und passt auf der gesyncten Box vakuum, weil dort nichts gestaged ist): `iter.sh cmd 'npx tsx scripts/checkFilenames.ts libs/src/tldraw-sync/constants/tldrawRoomIdSegmentPattern.ts libs/src/tldraw-sync/utils/isValidTldrawRoomId.ts libs/src/tldraw-sync/utils/namespaceLegacyTldrawAssetUrl.ts'`
- `iter.sh cmd 'npm run check-circular-deps'` sauber (`libs/src/tldraw-sync/utils` importiert nach `libs/src/whiteboard/constants` — der Kreis ist zu widerlegen, nicht anzunehmen)
i18n: keine
Doku: keine (intern)
Abhängt von: —

### T6b — API: `assertRoomAccess` in TLDrawSyncService portieren (ohne sie kompiliert T7 nicht)  [ ]
Komponente: apps/api · Dateien: `apps/api/src/tldraw-sync/tldraw-sync.service.ts` (Z. 151-178), `apps/api/src/tldraw-sync/tldraw-sync.service.spec.ts` (neu)
**Warum diese Task existiert:** T7 behauptete, `assertRoomAccess` „existiert im Fork bereits". Das ist **falsch** — `grep -rn "assertRoomAccess" apps libs` liefert **0 Treffer**; der Service hat nur `getPermittedUsers` (:151) und eine Inline-Prüfung in `getHistory` (:165-167, `if (!permittedUsers.some(…)) return undefined;`).
Soll: NEW:74414-74429 — `async assertRoomAccess(roomId, username)`: bei Prefix `single-` `return`, wenn `roomId === `${TLDRAW_SINGLE_USER_ROOM_PREFIX}${username}``, sonst 403; bei Prefix `multi-` `getPermittedUsers(roomId)` und `return` bei Treffer, sonst 403; ohne Prefix 403. Alle drei Würfe identisch: `new CustomHttpException(AUTH_ERROR_MESSAGES.Forbidden, HttpStatus.FORBIDDEN, roomId, TLDrawSyncService.name)`.
Änderung: Methode 1:1 ergänzen, ausschließlich mit vorhandenen Bausteinen — `TLDRAW_MULTI_USER_ROOM_PREFIX` / `TLDRAW_SINGLE_USER_ROOM_PREFIX` aus `libs/src/whiteboard/constants/` (im Service bereits importiert), `AUTH_ERROR_MESSAGES.Forbidden` (`libs/src/auth/constants/authErrorMessages.ts:23`, existiert und ist in DE/EN/FR übersetzt), `CustomHttpException`. **Keine neuen Fehlerkeys erfinden** (`scripts/checkErrorMessages.ts` prüft sie über alle drei Locales). Statischer Logger, keine Kommentare.
`getHistory` (:165-167) auf die neue Methode refaktorieren: statt `if (!permittedUsers.some(…)) return undefined;` → `await this.assertRoomAccess(roomIdWithPrefix, username);`. **Bewusste, SOLL-treue Verhaltensänderung:** aus „gibt `undefined` zurück" wird „wirft 403". Der FE-Consumer (`useTLDRawHistoryStore`) muss den 403 über `handleApiError` verarbeiten — vor dem Commit prüfen und, falls der Store auf `undefined` baut, im selben Commit nachziehen.
Spec `tldraw-sync.service.spec.ts` (neu, `getPermittedUsers` gemockt): (a) `('single-alice','alice')` → resolved; (b) `('single-bob','alice')` → `CustomHttpException` mit `HttpStatus.FORBIDDEN`; (c) `('multi-7a','alice')` mit `alice` in der Liste → resolved; (d) `('multi-7a','mallory')` bei leerer Liste → 403; (e) `('ohne-prefix','alice')` → 403 und `getPermittedUsers` **nicht** aufgerufen; (f) `getHistory` mit unberechtigtem User wirft 403, statt `undefined` zurückzugeben.
Verify: `iter.sh cmd 'npx nx run api:test -- --testPathPattern=tldraw-sync.service'` grün inkl. (a)-(f); `iter.sh cmd 'npm run build:api'` grün; `iter.sh cmd 'c=$(grep -c assertRoomAccess apps/api/src/tldraw-sync/tldraw-sync.service.ts); echo "impl=$c"; [ "$c" -ge 2 ]'` (Definition **und** Aufruf aus `getHistory`).
i18n: keine (`auth.errors.Forbidden` liegt in DE/EN/FR)
Doku: keine (intern)
Abhängt von: —

### T7 — API: WhiteboardAssetAccessGuard (schließt die offene Asset-Lücke)  [ ]
Komponente: apps/api · Dateien: `apps/api/src/tldraw-sync/whiteboardAssetAccess.guard.ts` (neu), `apps/api/src/tldraw-sync/whiteboardAssetAccess.guard.spec.ts` (neu), `apps/api/src/tldraw-sync/tldraw-sync.module.ts`
Soll: NEW:75404-75435 (`WhiteboardAssetAccessGuard`, `@Injectable()`, konstruktor-injizierter `TLDrawSyncService`, `canActivate` liest `request.params[ROOM_ID_PARAM]`, wirft `CustomHttpException(AUTH_ERROR_MESSAGES.Forbidden, HttpStatus.FORBIDDEN, roomId, WhiteboardAssetAccessGuard.name)` bei ungültiger Room-ID, sonst `getUsernameFromRequest(request)` + `await this.tldrawSyncService.assertRoomAccess(roomId, username)`) · Provider-Wiring NEW:74281 (`providers: [TLDrawSyncService, TLDrawSyncGateway, WhiteboardAssetAccessGuard]`) · `assertRoomAccess` NEW:74414-74429 — **existiert im Fork NICHT** (`grep -rn "assertRoomAccess" apps libs` = 0 Treffer; der Service hat nur `getPermittedUsers` :151). Die Methode kommt aus der neuen **T6b**.
Änderung: Neue Guard-Datei mit AGPL-SPDX-Header, Default-Export am Ende. `canActivate(context: ExecutionContext)`: Request aus `context.switchToHttp().getRequest()`, `roomId = request.params[ROOM_ID_PARAM]`; `if (!isValidTldrawRoomId(roomId)) throw new CustomHttpException(...)`; Username über den verifizierten Helper `apps/api/src/common/utils/getUsernameFromRequest.ts` (:23) — dieselbe Quelle, die `apps/api/src/common/decorators/getCurrentUsername.decorator.ts:22` importiert; **keinen neuen erfinden**. `isValidTldrawRoomId` kommt aus T6, `assertRoomAccess` aus **T6b** (im Fork nicht vorhanden); dann `await this.tldrawSyncService.assertRoomAccess(roomId, username)`; `return true`. Guard in `tldraw-sync.module.ts` als Provider registrieren (sonst kann `@UseGuards` ihn nicht auflösen). Der Guard wird in T9 an die Routen gehängt — hier **noch nicht**.
Spec: `assertRoomAccess` gemockt. (a) `params.roomId = 'multi-7a'`, Service resolved → `canActivate` true; (b) `params.roomId = '../etc'` → `CustomHttpException` mit Status 403, Service **nicht** aufgerufen; (c) `params.roomId = 'ohne-prefix'` → 403; (d) Service wirft → Fehler propagiert unverändert (kein Verschlucken).
Verify: `iter.sh cmd 'npx nx run api:test -- --testPathPattern=whiteboardAssetAccess'` grün inkl. der vier Assertions; `iter.sh cmd 'npm run build:api'` grün; `iter.sh cmd 'c=$(grep -c WhiteboardAssetAccessGuard apps/api/src/tldraw-sync/tldraw-sync.module.ts); echo "wiring=$c"; [ "$c" = 2 ]'` — Import **und** Provider-Eintrag (heute steht dort `providers: [TLDrawSyncService, TLDrawSyncGateway]`, Z. 33; ein einzelner Import allein reicht nicht).
i18n: keine
Doku: keine (intern) — der Sicherheitsbefund wird in T9 dokumentiert
Abhängt von: T6, **T6b** (ohne `assertRoomAccess` kompiliert der Guard nicht)

### T8 — API: multer-Utilities mit basePath-Containment (6 + 2 Aufrufstellen)  [ ]
Komponente: apps/api · Dateien: `apps/api/src/filesystem/multer.utilities.ts`, `apps/api/src/surveys/surveys.controller.ts` (Z. 141, 259), `apps/api/src/surveys/public-surveys.controller.ts` (Z. 121), `apps/api/src/filesystem/filesystem.controller.ts` (Z. 68, 158), `apps/api/src/bulletinboard/bulletinboard.controller.ts` (Z. 84), `apps/api/src/tldraw-sync/tldraw-sync.controller.ts` (Z. 69), `apps/api/src/filesystem/multer.utilities.spec.ts` (neu)
Soll: NEW:65960-65983 `createDiskStorage(basePath, getDestinationPath, fileNameGenerator)` — löst Zielordner und `basePath` mit `resolve()` auf und ruft `callback(new BadRequestException(PathValidationErrorMessages.OutsidePublicDirectory), '')`, wenn der Zielordner nicht `resolvedBase + sep` präfixiert ist bzw. ihm gleicht; sanitisiert außerdem den Dateinamen via `basename()` + Sanitizer · NEW:65995-66000 `createAttachmentUploadOptions(basePath, getDestinationPath, filter = true, fileNameGenerator, maxFileSize)`.
Änderung: Beide Fabriken bekommen `basePath: string` als **ersten** Parameter (Contract-Sync: alle 6 Aufrufstellen ziehen mit, die Surgical-Regel gilt hier nicht). In `createDiskStorage` vor `mkdirSync` die Containment-Prüfung ergänzen (`path.resolve` auf beide Seiten, Vergleich mit `path.sep`) und im `filename`-Callback `basename(fileName)` durch den im Fork vorhandenen `sanitizeFileName` aus `@libs/filesystem/utils/sanitizeFileName` schicken (**kein** neuer Sanitizer — `sanitizePath` aus dem SOLL existiert im Fork nicht, `sanitizeFileName` ist das Pendant und wird bereits von `ValidatePathPipe` benutzt). Die 6 Aufrufer bekommen als `basePath` genau das Wurzelverzeichnis, unter dem ihr `getDestinationPath` liegt (bei tldraw-sync: `WHITEBOARD_FILES_PATH`; bei den anderen: das jeweils schon im Aufruf verwendete App-Verzeichnis) — keine sonstigen Verhaltensänderungen an diesen Controllern.
Spec (neu): `createDiskStorage('/base', () => '/base/room1')` → `destination`-Callback ruft `callback(null, '/base/room1')`; `createDiskStorage('/base', () => '/other')` → `callback` bekommt eine `BadRequestException` mit `PathValidationErrorMessages.OutsidePublicDirectory`; `createDiskStorage('/base', () => '/basement')` (Präfix-String, aber anderer Ordner) → ebenfalls abgelehnt; `filename`-Callback mit `originalname` `'../../x.png'` liefert einen Namen ohne `..` und ohne `/`.
Verify: `iter.sh test:api` grün inkl. `multer.utilities.spec.ts` (vier Assertions oben) und weiterhin grün für alle bestehenden Controller-Specs; `iter.sh cmd 'npm run build:api'` grün; `iter.sh cmd 'a=$(grep -rho "createAttachmentUploadOptions(" apps/api/src --include="*.ts" | wc -l); d=$(grep -rho "createDiskStorage(" apps/api/src --include="*.ts" | wc -l); echo "aufrufe=$a disk=$d"; [ "$a" = 6 ] && [ "$d" = 2 ]'` (verifizierter Ist-Stand: 6 Aufrufe von `createAttachmentUploadOptions` — public-surveys:121, surveys:141/:259, filesystem:68, tldraw-sync:69, bulletinboard:84 — plus 2 von `createDiskStorage` in multer.utilities:76 und filesystem.controller:158; die Zahlen dürfen sich durch T8 **nicht** ändern). **Ehrlich benannt:** diese Klammer ist vor und nach der Änderung erfüllt; das eigentliche Netz gegen eine vergessene Aufrufstelle ist `npm run build:api` — der neue Pflicht-Parameter `basePath` macht jede nicht nachgezogene Stelle zum Typfehler.
i18n: keine
Doku: keine (intern)
Abhängt von: —

### T9 — API: /tldraw-sync/assets/* → /assets/:roomId/* inkl. Guard  [ ]
Komponente: apps/api · Dateien: `apps/api/src/tldraw-sync/tldraw-sync.controller.ts`, `apps/api/src/tldraw-sync/tldraw-sync.controller.spec.ts`
Soll: NEW:75238-75344. Konkret: `uploadFileToApp` = `@Post(\`${ASSETS}/:${ROOM_ID_PARAM}/:name\`)` + `@UseGuards(WhiteboardAssetAccessGuard)` + `FileInterceptor('file', createAttachmentUploadOptions(WHITEBOARD_FILES_PATH, (req) => join(WHITEBOARD_FILES_PATH, sanitize(req.params[ROOM_ID_PARAM] ?? '')), false, (_req, file) => file.originalname))` (NEW:75264-75287) · `serveFiles` = `@Get(\`${ASSETS}/:${ROOM_ID_PARAM}/*filename\`)` + Guard, Body `serveFile(APPS.WHITEBOARD, join(roomId, FilesystemService.buildPathString(filename)), req, res)` (NEW:75288-75307, Handler NEW:75251-75254) · `deleteFile` = `@Delete(\`${ASSETS}/:${ROOM_ID_PARAM}/*filename\`)` + Guard, Body `FilesystemService.deleteFile(WHITEBOARD_FILES_PATH, join(roomId, FilesystemService.buildPathString(filename)))` (NEW:75308-75321, Handler NEW:75255-75257) · `getHistory` bleibt `@Get(\`${HISTORY}/:${ROOM_ID_PARAM}\`)` unverändert (NEW:75322-75336).
Änderung: Die drei Asset-Routen um das `:roomId`-Segment erweitern, `@Param(ROOM_ID_PARAM) roomId: string` als **ersten** Handler-Parameter aufnehmen und `@UseGuards(WhiteboardAssetAccessGuard)` je Route setzen. **Das ist zugleich ein Sicherheits-Fix:** die drei Routen haben im Fork heute *überhaupt keinen* Room-Guard — jeder Whiteboard-berechtigte Nutzer kann Assets fremder Räume lesen und löschen. Der Guard darf nicht weggelassen oder auf Controller-Ebene „vereinfacht" werden. Reihenfolge-Argument fürs Review: NestJS führt Guards **vor** Interceptors aus, deshalb ist `req.params[ROOM_ID_PARAM]` in der multer-`destination`-Funktion bereits gegen `TLDRAW_ROOM_ID_SEGMENT_PATTERN` validiert; die zusätzliche Containment-Prüfung aus T8 ist die zweite Verteidigungslinie. `createAttachmentUploadOptions` mit der neuen Signatur aus T8 aufrufen (`WHITEBOARD_FILES_PATH` als `basePath`, Destination `join(WHITEBOARD_FILES_PATH, roomId)`). `ROOM_ID_PARAM` kommt aus dem bestehenden `@libs/tldraw-sync/constants/roomIdParam` — keine magic strings. Swagger-`@ApiParam({ name: ROOM_ID_PARAM, ... })` je Route ergänzen, den restlichen Swagger-Bestand nicht umbauen.
Spec-Erweiterung (bestehende Datei): (a) `serveFiles('multi-7a', 'a.png', req, res)` ruft `filesystemService.serveFile` mit einem Pfad auf, der mit `multi-7a/` beginnt; (b) `deleteFile('multi-7a','a.png')` ruft `FilesystemService.deleteFile` mit `WHITEBOARD_FILES_PATH` und einem `multi-7a/`-Pfad auf; (c) Auth-Contract via `controllerContractReflection.getRouteGuards(TLDrawSyncController, 'serveFiles'|'deleteFile'|'uploadFileToApp')` enthält jeweils `WhiteboardAssetAccessGuard`; (d) die bestehenden `uploadFileToApp`-Tests bleiben grün.
**Bestand, der erhalten bleiben muss (nicht neu bauen, nicht wegoptimieren):** `serveFiles` (:85) und `deleteFile` (:93) tragen heute schon `@Param('filename', new ValidatePathPipe(WHITEBOARD_FILES_PATH))`. Diese Pipe bleibt **unverändert** auf dem `filename`-Parameter stehen — sie ist neben Guard (T7) und Containment (T8) die dritte Verteidigungslinie.
Verify: `iter.sh cmd 'npx nx run api:test -- --testPathPattern=tldraw-sync.controller'` grün inkl. (a)-(d); `iter.sh cmd 'npm run build:api'` grün; `iter.sh cmd 'npm run check-spec-coverage'` sauber; und der eigentliche Auth-Nagel — `iter.sh cmd 'f=apps/api/src/tldraw-sync/tldraw-sync.controller.ts; r=$(grep -c "ASSETS}/:" "$f"); p=$(grep -c "ValidatePathPipe(WHITEBOARD_FILES_PATH)" "$f"); g=$(grep -c "UseGuards(WhiteboardAssetAccessGuard)" "$f"); echo "routen=$r pipes=$p guards=$g"; [ "$r" = 3 ] && [ "$p" = 2 ] && [ "$g" = 3 ]'` → heute `routen=1 pipes=2 guards=0` (rot), nach korrekter Umsetzung `3/2/3`. Eine weggelassene Pipe oder ein fehlender Guard kippt ihn zurück auf rot.
i18n: keine
Doku: `docs/adr/` Kurz-Eintrag oder Abschnitt im Bundle-Diff-Report: Route-Bruch `assets/<file>` → `assets/<roomId>/<file>` + geschlossene Guard-Lücke + Hinweis, dass alte Clients 404/403 bekommen
Abhängt von: T6, T7, T8

### T10 — API: TldrawSyncRoom.schemaVersion + Disk-Migration 000-namespace-assets-by-room  [ ]
Komponente: apps/api · Dateien: `apps/api/src/tldraw-sync/tldraw-sync-room.schema.ts`, `apps/api/src/tldraw-sync/migrations/migration000NamespaceAssetsByRoom.ts` (neu), `apps/api/src/tldraw-sync/migrations/tldrawSyncRoomMigrationsList.ts` (neu), `apps/api/src/tldraw-sync/tldraw-sync.service.ts`, `apps/api/src/tldraw-sync/migrations/migration000NamespaceAssetsByRoom.spec.ts` (neu)
Soll: NEW:74318-74338 (`TldrawSyncRoom` mit drittem Feld `@Prop({ required: false }) schemaVersion: number`) · NEW:74742-74806 (`name = '000-namespace-assets-by-room'`, `schemaVersion = 1`, `ASSET_URL_PROPS = ['src','url']`, `moveLegacyAssetFile`, sequentielles `reduce` über Räume/Dokumente/Props, `updateOne({roomId}, hasChanges ? {$set:{roomData, schemaVersion}} : {$set:{schemaVersion}})`, `Logger.error` je Raum im catch, Abschluss-Log) · NEW:74703-74706 (`tldrawSyncRoomMigrationsList = [migration000NamespaceAssetsByRoom]`) · NEW:74411-74413 (`async onModuleInit() { await MigrationService.runMigrations(this.roomModel, tldrawSyncRoomMigrationsList); }`).
Änderung: **Destruktiv — verschiebt Dateien auf der Platte.** Vor jedem Lauf gegen echte Daten: Dump **+ `./data/master.key`**. Forward-only, kein Rollback.
- Schema: Feld `schemaVersion?: number` mit `@Prop({ required: false })` ergänzen (`@Schema({ timestamps: true, strict: true })` bleibt).
- Migration: neue Datei mit AGPL-SPDX-Header. `moveLegacyAssetFile(encodedFilename, roomId)`: `diskName = sanitizeFileName(decodeURIComponent(encodedFilename))`; früher Ausstieg bei leerem Namen; `source = join(WHITEBOARD_FILES_PATH, diskName)`, `destination = join(WHITEBOARD_FILES_PATH, roomId, diskName)`; **Skip**, wenn Quelle fehlt **oder** Ziel bereits existiert (Idempotenz); sonst `ensureDir` + `move` (`fs-extra`, bereits Dependency). Selektor: `find({ $or: [{ schemaVersion: { $exists: false } }, { schemaVersion: { $lt: 1 } }] })`. Pro Raum über `roomData.documents[].state.props` iterieren, für `src` und `url` `namespaceLegacyTldrawAssetUrl(value, roomId)` (T6) anwenden, bei Treffer Datei verschieben und `props[propName] = namespaced.newUrl` setzen. Alle Schleifen als sequentielles `await`-`reduce` (kein `Promise.all` — die Dateisystem-Moves dürfen sich nicht überholen). Fehler pro Raum fangen und `Logger.error(..., MigrationService.name)`, Migration läuft weiter. Statische Logger, keine Kommentare im Code.
- Liste + `onModuleInit` in `TLDrawSyncService` (der Service hat heute keinen — neu ergänzen, `OnModuleInit` implementieren) analog zu `global-settings.service.ts:51` / `surveys.service.ts:57`.
Spec: In-Memory-Mock für `model.find/updateOne` + gemocktes `fs-extra` (`pathExists`, `ensureDir`, `move`). (a) Raum ohne `schemaVersion` mit `src = '/edu-api/tldraw-sync/assets/a.png'` → `move` von `<base>/a.png` nach `<base>/multi-7a/a.png`, `props.src` endet auf `/assets/multi-7a/a.png`, `$set` enthält `roomData` **und** `schemaVersion: 1`; (b) bereits namespacete URL → kein `move`, `$set` enthält nur `schemaVersion`; (c) Ziel existiert bereits → kein `move`, URL wird trotzdem umgeschrieben; (d) Quelle fehlt → kein `move`, kein Absturz; (e) `move` wirft → `Logger.error`, die übrigen Räume werden weiter verarbeitet; (f) zweiter Lauf über dieselben Daten ändert nichts (Idempotenz).
Verify: `iter.sh test:api` grün inkl. (a)-(f); `iter.sh cmd 'npm run build:api'` grün. Voll-Stack-Nachweis (Dateien tatsächlich verschoben, Whiteboard-Bilder laden weiter) ist box-gated → an den nächsten `iter.sh deploy` koppeln und hier als offen vermerken.
i18n: keine
Doku: Eintrag in `docs/migrations/` — Modell `TldrawSyncRoom`, 0→1, **verschiebt Dateien auf der Platte**, forward-only, Rollback = Dump **+ `./data/master.key`** + Rück-Move von Hand
Abhängt von: T6, T9

### T11 — FE: TLDrawWithSync auf raum-namespacete Asset-Pfade  [ ]
Komponente: apps/frontend · Dateien: `apps/frontend/src/pages/Whiteboard/TLDrawWithSync/TLDrawWithSync.tsx` (Z. 64, 72-83, 118, 133), `libs/src/tldraw-sync/utils/resolveTldrawRoomId.ts` (neu), `libs/src/tldraw-sync/utils/resolveTldrawRoomId.spec.ts` (neu)
Soll: **Kein un-minifizierter FE-Quellcode.** Die *Struktur* ist aus dem minifizierten Chunk `.reference/2.1.0/ui/assets/TLDrawWithSync-91KtZc9A.js` belegbar; Bezeichner und Dateinamen sind **fork-original**. Belegte Struktur: `var ze='single-', Be=(e,t)=>e?\`${c}${e}\`:\`${ze}${t}\`` (Resolver aus selectedRoomId + Username, `c` = `'multi-'`); `a=useRef(Be(void 0, user?.username ?? 'guest'))` plus `useEffect(()=>{a.current=Be(b, user?.username ?? 'guest')},[b, user?.username])`; Upload-Pfad `` `${T}/${encodeURIComponent(a.current)}/${encodeURIComponent(n)}` `` mit `T = \`/${BASE}/${ASSETS}\``; Delete-Pfad `` `${T}/${s}/${encodeURIComponent(c)}` `` mit `a=url.split('/'); o=a.pop(); s=a.pop(); c=decodeURIComponent(o)`. Zum Vergleich der 2.0.200-Chunk `TLDrawWithSync-DcBjqCof.js`: Upload `` `${w}/${encodeURIComponent(n)}` ``, Delete `decodeURIComponent(r.split('/').pop())` — die Differenz ist **exakt** das Raum-Segment. Der API-Contract dahinter ist NEW:75264-75321.
Änderung: Neuer libs-Helper `resolveTldrawRoomId(selectedRoomId: string | undefined, username: string): string` — `selectedRoomId ? TLDRAW_MULTI_USER_ROOM_PREFIX + selectedRoomId : TLDRAW_SINGLE_USER_ROOM_PREFIX + username` (identische Ableitung wie das Gateway in `tldraw-sync.gateway.ts:162-164`, damit FE und BE denselben Raumnamen bilden). AGPL-SPDX-Header, Default-Export am Ende, Dateiname == Export-Name.
In `TLDrawWithSync.tsx`: `const roomIdRef = useRef(resolveTldrawRoomId(undefined, user?.username ?? 'guest'))`, aktualisiert in einem `useEffect` auf `[selectedRoomId, user?.username]` (Ref statt State, weil `assetStore` in `useMemo` mit stabiler Dependency-Liste gehalten wird — sonst reißt `useSync` den Store bei jedem Raumwechsel neu auf). Upload (Z. 72): `const assetPath = \`${assetBasePath}/${encodeURIComponent(roomIdRef.current)}/${encodeURIComponent(filename)}\``. Delete-Handler (Z. 118/133): aus `url` erst den Dateinamen, dann das Raum-Segment poppen und `eduApi.delete(\`${assetBasePath}/${roomSegment}/${encodeURIComponent(fileName)}\`)` aufrufen — das Segment aus der URL nehmen, **nicht** aus `roomIdRef`, damit auch nach einem Raumwechsel die richtige Datei gelöscht wird. `eduApi` bleibt der Transport (kein `fetch`), `handleApiError` bleibt. Ausdrücklich **nicht** mitbauen: die Bild-Kompression vor dem POST und das `renderLabel`-Prop von `DropdownSelect` (beides in 2.1.0 vorhanden, gehört in andere Pakete — in T3 als Befund festhalten).
Spec (`resolveTldrawRoomId.spec.ts`, vitest): `('7a','alice')` → `'multi-7a'`; `(undefined,'alice')` → `'single-alice'`; `('','alice')` → `'single-alice'`; `(undefined,'guest')` → `'single-guest'`.
Verify: `iter.sh test:frontend` grün **und** die libs-Spec ist nachweislich gelaufen (sie liegt unter `libs/` und wird ohne den `test.include`-Fix aus T6 gar nicht eingesammelt): `iter.sh cmd 'npx nx test frontend --run 2>&1 | tee /tmp/vt11.log; grep -q resolveTldrawRoomId /tmp/vt11.log && echo SPEC-RAN'` → `SPEC-RAN`; `iter.sh cmd 'npx tsc -p apps/frontend/tsconfig.app.json --noEmit'` grün (**Pflicht** — eslint+vitest verdecken fehlende Props); `iter.sh lint` sauber; `iter.sh cmd 'npm run build'` grün. Funktionaler Nachweis (Bild einfügen → POST auf `/tldraw-sync/assets/<prefixed-roomId>/<file>` → 200, Reload zeigt das Bild, Löschen entfernt genau diese Datei) ist box-gated → an den nächsten `iter.sh deploy`/`shots` koppeln.
i18n: keine
Doku: keine (der Route-Bruch ist in T9 dokumentiert)
Abhängt von: T9

### T12 — API: GET mails/domains statt mails/mailcow-mailboxes/domains  [?] human-gate: Duplikat zu `p4-mail-rework-21-sieve` T39/T40 — die Guard-Frage ist dort **bereits entschieden**
**Vorrang-Hinweis (vor jeder Umsetzung lesen):** `tasks/p4-mail-rework-21-sieve.md:317` (T39) baut exakt dieselbe Route um und entscheidet ausdrücklich **„Fork behält den `AdminGuard`"**, mit ADR in T40 (`docs/adr/0002-mails-domains-admin-guard.md`). Diese Task hier plante das Gegenteil (Guard entfernen). Solange Kevin die p4-Entscheidung nicht widerruft, ist T12 **nicht** umzusetzen — der Pfadwechsel gehört zu p4 T39. Bei Widerruf: hier nur die Pfad-Umstellung, die Guard-Aufweitung als eigener, ADR-pflichtiger Schritt.
Komponente: apps/api · Dateien: `apps/api/src/mails/mails.controller.ts` (Z. 143-147), `apps/api/src/mails/mails.controller.spec.ts` (Z. 43-56)
Soll: NEW:28387-28394 — `@Get(MAIL_ENDPOINT_PATHS.DOMAINS)` mit `@ApiOperation({ summary: "List the organization's mail domains (sender classification and mailbox creation)" })` und `@ApiResponse({ status: HttpStatus.OK, description: 'List of the organization mail domains', type: [String] })`, **ohne `@UseGuards`**. Handler NEW:27881-27883. Konstante `DOMAINS: 'domains'` NEW:28917 (existiert im Fork bereits: `libs/src/mail/constants/mailEndpointPaths.ts:21`). Controller-Ebene NEW (MailsController-Decorators): `ApiTags`, Auth-Decorator, `RequireAppAccess(APPS.MAIL)`, `Controller(MAIL_ENDPOINT)` — die drei Mailcow-Admin-Routen daneben behalten in 2.1.0 ihren `AdminGuard` (drei `UseGuards(admin_guard)` im Controller-Bereich).
Änderung: Route-Pfad von `` `${MAIL_ENDPOINT_PATHS.MAILCOW_MAILBOXES}/${MAIL_ENDPOINT_PATHS.DOMAINS}` `` auf `MAIL_ENDPOINT_PATHS.DOMAINS` umstellen und die beiden Swagger-Decorators aus dem SOLL ergänzen. **`@UseGuards(AdminGuard)` fällt weg** — das ist eine bewusste Zugriffs-Aufweitung (2.1.0 braucht die Domainliste für die Absender-Klassifikation im normalen Mail-Client, nicht nur im Admin-Panel) und **muss als Produktentscheidung sichtbar sein**: wenn Kevin sie nicht will, Task auf `[?] human-gate` setzen statt still zu entscheiden. Methodenname `getMailcowDomains` und die Delegation an `this.mailcowAdminService.getMailcowDomains()` bleiben (2.1.0 hat die Mailcow-Logik nach `MailsService` verschoben, NEW:30808 — das ist der p4-mail-Refactor und **nicht** Teil dieser Task). Die übrigen fünf `mailcow-mailboxes`-Routen bleiben unverändert admin-guarded.
Spec-Änderung: `getMailcowDomains` aus `ADMIN_GUARDED_ROUTES` entfernen und in `NON_ADMIN_ROUTES` aufnehmen — damit nagelt der bestehende Auth-Contract-Test (`getRouteGuards(...).not.toContain(AdminGuard)`) die Entscheidung fest, statt sie zu verschweigen. Zusätzlich eine Assertion, dass der Route-Pfad exakt `'domains'` ist (via `Reflect.getMetadata(PATH_METADATA, MailsController.prototype.getMailcowDomains)`), damit ein Rückfall auf den alten Pfad rot wird.
Verify: `iter.sh test:api` grün inkl. beider geänderter Contract-Blöcke und der neuen Pfad-Assertion; `iter.sh cmd 'npm run build:api'` grün; `iter.sh cmd "grep -n 'MAILCOW_MAILBOXES}/\${MAIL_ENDPOINT_PATHS.DOMAINS' apps/api/src/mails/mails.controller.ts"` → **keine Treffer**. Live-Nachweis (`GET /edu-api/mails/domains` als Nicht-Admin → 200, alter Pfad → 404) ist box-gated.
i18n: keine
Doku: `docs/mailcow-admin.{de,en,fr}.md` — Route-Umbenennung und die Guard-Aufweitung inkl. Begründung in allen drei Sprachen nachziehen
Abhängt von: —

### T13 — FE: useMailsStore auf GET mails/domains  [?] human-gate: hängt vollständig an T12 (dort: Duplikat zu p4 T39, Guard-Frage bereits entschieden)
Komponente: apps/frontend · Dateien: `apps/frontend/src/pages/Mail/useMailsStore.ts` (Z. 43, 150), `apps/frontend/src/pages/Mail/useMailsStore.spec.ts` (Z. 48-56)
Soll: API-Contract aus T12 (NEW:28387-28394). **Der FE-Consumer selbst ist fork-original** — für 2.1.0 gibt es keinen FE-Quellcode, nur das minifizierte Bundle.
Änderung: In `useMailsStore.ts` eine Konstante `const DOMAINS_PATH = \`${MAILS_PATH}/${MAIL_ENDPOINT_PATHS.DOMAINS}\`;` neben dem bestehenden `MAILCOW_MAILBOXES_PATH` anlegen (Z. 43er-Block, keine magic strings) und in `getMailcowDomains` (Z. 150) `eduApi.get<string[]>(DOMAINS_PATH)` statt `` `${MAILCOW_MAILBOXES_PATH}/${MAIL_ENDPOINT_PATHS.DOMAINS}` `` aufrufen. `MAILCOW_MAILBOXES_PATH` bleibt für die fünf übrigen Mailcow-Aktionen. Store-Signatur, `isMailcowLoading`-Handling und `handleApiError` unverändert; `MailcowAdminPanel.tsx` ruft weiter `getMailcowDomains()` — dort keine Änderung.
Verify: `iter.sh test:frontend` grün — der bestehende Test „getMailcowDomains fetches the domains route and stores the result" prüft `expect(mockedEduApi.get).toHaveBeenCalledWith(DOMAINS_PATH)`; die im Spec importierte/erwartete Konstante auf den neuen Pfad ziehen und zusätzlich hart assertieren, dass der aufgerufene Pfad **nicht** `mailcow-mailboxes` enthält. Dazu `iter.sh cmd 'npx tsc -p apps/frontend/tsconfig.app.json --noEmit'` grün und `iter.sh lint` sauber.
i18n: keine
Doku: keine (in T12 abgedeckt)
Abhängt von: T12

### T14 — React-19-Vorarbeiten, gültig noch unter React 18  [ ]
Komponente: apps/frontend, libs · Dateien: `apps/frontend/src/pages/LoginPage/useSilentLoginWithPassword.ts` (Z. 37), `apps/frontend/src/pages/ClassManagement/components/Dialogs/CollectFilesDialog.tsx` (Z. 36), `apps/frontend/src/components/ui/Table/SelectableCell.tsx` (Z. 30, 122), `apps/frontend/src/components/structure/layout/NativeAppPageManager.tsx` (Z. 33), `apps/frontend/src/components/shared/Card/Card.tsx` (Z. 64), `apps/frontend/src/components/ui/Dialog.tsx` (Z. 102-104), `apps/frontend/src/components/ui/DropdownMenuSH.tsx` (Z. 208-218), `apps/frontend/src/components/ui/Checkbox.tsx` (Z. 71-73), `apps/frontend/src/components/ui/Breadcrumb.tsx` (Z. 119-121), `libs/ui-kit/src/components/Button.tsx` (Z. 108)
Soll: **Kein Bundle-Anker möglich** — betrifft ausschließlich Fork-Quellcode, den es für 2.1.0 nicht im Klartext gibt. Fork-originale Vorarbeit, motiviert durch die belegte React-19-Umstellung (Sentinel `__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE` im 2.1.0-Bundle gegen `__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED` in 2.0.200).
Änderung: Drei kleine, unter React 18 **bereits gültige** Anpassungen, damit T15 nicht gleichzeitig Bump und Codemod ist:
1. `useRef<Keycloak>()` → `useRef<Keycloak | undefined>(undefined)` (in `@types/react` 19 ist das Argument von `useRef` Pflicht). Genau eine Stelle im Baum.
2. Fünf bare `JSX.Element` → `React.JSX.Element` (der globale `JSX`-Namespace entfällt in `@types/react` 19). Falls `React` in einer Datei nicht importiert ist, den vorhandenen Import-Stil der Datei übernehmen.
3. Sieben `.defaultProps`-Zuweisungen an Funktionskomponenten durch Default-Parameterwerte in der Destrukturierung ersetzen (React 19 ignoriert `defaultProps` an Funktionskomponenten zur Laufzeit): `DialogContent` (`showCloseButton = true`), `DropdownMenuSubTrigger`/`DropdownMenuItem`/`DropdownMenuLabel` (`inset = false`), `Checkbox` (`onCheckboxClick = () => {}`), `BreadcrumbLink` (`asChild = false`), `libs/ui-kit/src/components/Button.tsx` (das dortige `defaultProps`-Objekt in Default-Parameter auflösen). Die `.displayName`-Zuweisungen bleiben unangetastet. Kein Umformatieren fremder Zeilen, keine Kommentare.
Verify: `iter.sh cmd 'npx tsc -p apps/frontend/tsconfig.app.json --noEmit'` grün; `iter.sh test:frontend` grün (30 Specs unverändert); `iter.sh lint` sauber; `iter.sh cmd 'npm run build'` grün; `iter.sh cmd "grep -rn '\.defaultProps' apps/frontend/src libs --include='*.tsx' | wc -l"` → `0`; `iter.sh cmd "grep -rn 'JSX.Element' apps/frontend/src libs --include='*.tsx' --include='*.ts' | grep -v 'React.JSX' | wc -l"` → `0`; `iter.sh cmd "grep -rn 'useRef<[^>]*>()' apps/frontend/src libs --include='*.ts' --include='*.tsx' | wc -l"` → `0`.
i18n: keine
Doku: keine (intern)
Abhängt von: —

### T15 — React 18 → 19 + react-helmet-async entfernen  [ ]
Komponente: apps/frontend, libs · Dateien: `package.json`, `package-lock.json`, `libs/ui-kit/package.json`, `apps/frontend/src/components/PageTitle.tsx`, `apps/frontend/src/App.tsx` (Z. 27, 80, 85)
Soll: Bundle-Evidenz statt Quellcode (das 2.1.0-FE liegt nur gebaut vor): React-19-Sentinel `__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE` in `.reference/2.1.0/ui/assets/*.js` (2.0.200: `__SECRET_INTERNALS_…`); die react-helmet-async-Marker `data-rh`, `helmetData`, `helmetInstances` sind in 2.0.200 vorhanden und in 2.1.0 **restlos verschwunden** (die verbleibenden Treffer `helmet-safety`/`helmet-un` sind Emoji-Namen). Die konkrete Ersatz-Implementierung ist **fork-original**.
Änderung: **Ein Commit** — der Peer-Konflikt macht Bump und Helmet-Ausbau untrennbar.
- `package.json`: `react`/`react-dom` → `^19.2.8`; devDeps `@types/react` → `^19.2.17`, `@types/react-dom` → `^19.2.3`; `react-helmet-async` **entfernen**.
- Neuer `overrides`-Block, damit die fünf peer-gedeckelten Pakete ohne Major-Bump im Baum bleiben (Bundle-Evidenz: 2.1.0 enthält weiterhin `rdp-vhidden` und kein `rdp-root`, upstream ist also auf react-day-picker **v8** geblieben): `"overrides": { "@types/react": "^19.2.17", "@types/react-dom": "^19.2.3", "cmdk": { "react": "$react", "react-dom": "$react-dom" }, "vaul": { "react": "$react", "react-dom": "$react-dom" }, "qrcode.react": { "react": "$react" }, "react-day-picker": { "react": "$react" }, "usehooks-ts": { "react": "$react" } }`. Die beiden `@types/*`-Overrides erzwingen **eine** Typen-Version im Baum (sonst kollidieren transitive `@types/react`@18 aus `@types/react-file-icon` & Co. und tsc wirft unauflösbare JSX-Fehler).
- `libs/ui-kit/package.json`: devDeps `@types/react`/`@types/react-dom`/`react`/`react-dom` auf `^19` ziehen. `peerDependencies` bleibt `>=18.0.0` (erfüllt 19 bereits) — nicht verengen, das ui-kit wird publiziert.
- `PageTitle.tsx`: `Helmet` entfernen, stattdessen React-19-natives Metadaten-Hoisting — die Komponente rendert direkt `<title>{…}</title>` (React 19 hebt es selbst in den `<head>`). Props-Interface (`title?`, `translationId`, `disableTranslation?`) und die Titel-Zusammensetzung (`title ? title + ' - ' : ''` + übersetzter Text + ` - ` + `APPLICATION_NAME`) bleiben **unverändert**, damit kein Aufrufer angefasst werden muss.
- `App.tsx`: `import { HelmetProvider }` und beide `<HelmetProvider>`-Tags entfernen; die Verschachtelung `TooltipProvider` → `AppRouter`/`ThemeToggle` bleibt sonst identisch.
- `package-lock.json` lokal per `npm install` neu erzeugen (die Dev-Box erreicht die Registry — verifiziert). Falls das lokal scheitert: auf der Box erzeugen und die Lockfile zurückholen (der crabbox-Sync ist einseitig).
Verify: `iter.sh cmd 'npm ci'` läuft sauber durch (kein `ERESOLVE`); `iter.sh cmd 'npm ls react react-dom @types/react @types/react-dom'` zeigt je **genau eine** Version, `react@19.2.x`; `iter.sh cmd "grep -rn 'react-helmet-async' apps libs package.json | grep -v node_modules | wc -l"` → `0`; `iter.sh cmd 'npx tsc -p apps/frontend/tsconfig.app.json --noEmit'` grün (**Pflicht**); `iter.sh test:frontend` grün; `iter.sh lint` sauber; `iter.sh cmd 'npm run build'` grün.
i18n: keine
Doku: Abschnitt „React 19" in `docs/features/p7-fe-toolchain-catchup.md` — warum `overrides` statt fünf Major-Bumps (inkl. der react-day-picker-Bundle-Evidenz), und dass der Helmet-Ersatz React-19-Hoisting ist
Abhängt von: T14

### T16 — tldraw 3.15.4 → 3.15.6  [ ]
Komponente: apps/frontend · Dateien: `package.json`, `package-lock.json`
Soll: Aus dem gebauten 2.1.0-Bundle direkt ablesbar — `L('@tldraw/sync','3.15.6','esm')` im Chunk `TLDrawWithSync-91KtZc9A.js`; alle neun tldraw-Pakete (`tldraw`, `@tldraw/{editor,state,state-react,store,sync,sync-core,tlschema,utils,validate}`) melden `3.15.6`, in 2.0.200 durchgehend `3.15.4`.
Änderung: `tldraw`, `@tldraw/sync`, `@tldraw/sync-core` von `^3.15.4` auf `^3.15.6` ziehen (Patch-Bump innerhalb 3.15, npm-Registry bestätigt `tldraw@3` = `3.15.6` als aktuelle 3.x). **Nicht** auf die aktuelle Major (5.2.5) gehen — das SOLL ist 3.15.6. Der `resolve.dedupe`-Block in `apps/frontend/vite.config.mts` (`@tldraw/store`, `@tldraw/validate`, `@tldraw/tlschema`) bleibt unverändert und muss nach dem Bump weiterhin genau eine Version je Paket auflösen. Lockfile lokal per `npm install` erneuern.
Verify: `iter.sh cmd 'npm ci'` grün; `iter.sh cmd "npm ls tldraw @tldraw/sync @tldraw/sync-core @tldraw/store @tldraw/tlschema @tldraw/validate"` zeigt je **eine** Version, alle `3.15.6`; `iter.sh cmd 'npx tsc -p apps/frontend/tsconfig.app.json --noEmit'` grün; `iter.sh test:frontend` grün; `iter.sh cmd 'npm run build'` grün. Zusatz-Assertion gegen das SOLL: `iter.sh cmd "grep -oh '\\\`@tldraw/sync\\\`,\\\`[0-9.]*\\\`' dist/apps/frontend/assets/*.js | sort -u"` liefert `3.15.6` — dieselbe Sonde, mit der die Referenz gemessen wurde.
i18n: keine
Doku: Zeile im Bundle-Diff-Report auf „umgesetzt" setzen
Abhängt von: T15

### T17 — Verifikationslauf React 19 gegen die Bundle-Sonden  [ ]
Komponente: apps/frontend · Dateien: keine Produktivdatei (nur Ledger-/Report-Fortschreibung in `docs/analysis/ui-bundle-diff-2.0.200-vs-2.1.0.md`)
Soll: Dieselben Sonden, mit denen 2.1.0 vermessen wurde — jetzt gegen den **eigenen** Build. Damit wird die Behauptung „wir sind auf React 19 wie 2.1.0" messbar statt geglaubt.
Änderung: Kein Code. Vollen Remote-Lauf fahren und die Sonden aus T2 auf `dist/apps/frontend/assets` anwenden; Ergebnis als Tabelle „Fork-Build vs. 2.1.0-Referenz" in den Report aus T3 nachtragen (React-Major, Helmet-Marker, tldraw-Version, ci-Farbklassen).
Verify: `iter.sh all` grün (lint + api-Tests + frontend-Tests + build:all + check-translations). Danach: `iter.sh cmd 'node --max-old-space-size=6000 scripts/reference/extractBundleFacts.mjs dist/apps/frontend/assets /tmp/fork.json && cat /tmp/fork.json'` liefert `react: 19`, `helmet: false`, `tldraw['@tldraw/sync']: '3.15.6'`, `ciClasses['text-ciDarkBlue'] > 0` und `ciClasses['text-ciLightBlue'] === 0` — also dieselbe Signatur wie `.reference/2.1.0`. Jede Abweichung ist ein Blocker, kein Restrisiko.
i18n: keine
Doku: Ergebnistabelle im Bundle-Diff-Report
Abhängt von: T4, T15, T16

### T18 — ESLint-Baseline: aufgelöste Konfiguration einfrieren  [ ]
Komponente: apps/frontend, apps/api, libs (Tooling) · Dateien: `docs/analysis/eslint-config-baseline-v8.md` (neu), `scripts/reference/eslintConfigSnapshot.sh` (neu)
Soll: Kein Bundle-Anker (Toolchain). Der Zweck ist ein **prüfbarer Maßstab**: die Flat-Config-Migration darf keine Regel still verlieren. Ausgangsstand: `eslint@^8.57.0`, `.eslintrc.json` (root) + `apps/frontend/.eslintrc.json` + `apps/api/.eslintrc.json` + `libs/ui-kit/.eslintrc.json`, `extends: [airbnb, airbnb-typescript, plugin:react/recommended, plugin:jsx-a11y/recommended, plugin:@typescript-eslint/recommended, plugin:@typescript-eslint/recommended-requiring-type-checking, prettier, plugin:import/recommended]`.
Änderung: Kleines Shell-Skript (SPDX-`#`-Header). **Der CLI-Vertrag ist verbindlich und muss so und nicht anders gebaut werden** — er ist die Voraussetzung dafür, dass die Abnahmekriterien von T19/T20 überhaupt etwas prüfen können:
```
usage: eslintConfigSnapshot.sh [<out-dir>]                  # Default: .crabbox/out/eslint-baseline
       eslintConfigSnapshot.sh --compare <dirA> <dirB>
```
Ohne das positionale `<out-dir>` schreiben die Läufe aus T19 und T20 in dasselbe Verzeichnis wie die Baseline, überschreiben sie, und `--compare baseline flat` vergleicht ein Verzeichnis mit sich selbst → leeres Delta, rc=0, **unbedingt grün**. Genau diese Falle ist zu vermeiden.
Snapshot-Modus: für drei repräsentative Dateien — je eine aus `apps/frontend/src`, `apps/api/src`, `libs/src` — `npx eslint --print-config <datei>` ausführen und nach `<out-dir>/<name>.json` schreiben (`frontend.json`, `api.json`, `libs.json`).
**Der Snapshot darf sich nicht auf `rules` beschränken:** gespeichert werden **vier** Blöcke — `rules`, `settings`, `plugins` und `parserOptions.project`. Ein reiner Regel-Diff kann Resolver- und Plugin-Drift konstruktiv nicht sehen, und genau die entscheidet in T19 darüber, ob `import/no-unresolved` auf jeden `@libs/*`-Import feuert. Normalisierung stabil sortiert per `node -e` — **nicht** per `jq`, das auf der crabbox nicht installiert ist (`warm.sh` bringt nur `rsync`/`git`).
`--compare <dirA> <dirB>` listet die Deltas je Block (neu / entfallen / Wert geändert) auf stdout und endet mit **rc=1 bei nicht-leerem Delta**, rc=0 sonst.
Die drei Snapshots als Anhang (Regel-Namen + Severity, `settings.import/resolver.typescript.project`-Liste, `plugins`-Array, ohne Options-Rauschen) in `docs/analysis/eslint-config-baseline-v8.md` committen, plus die Kopfzahlen (Anzahl aktiver Regeln je Projekt).
Verify:
- `iter.sh cmd 'bash scripts/reference/eslintConfigSnapshot.sh && n=$(ls .crabbox/out/eslint-baseline/*.json | wc -l); echo "snapshots=$n"; [ "$n" = 3 ]'`
- `iter.sh cmd 'r=$(node -pe "Object.keys(require(\"./.crabbox/out/eslint-baseline/frontend.json\").rules).length"); echo "rules=$r"; [ "$r" -gt 200 ]'`
- Positiv-Selbsttest: `iter.sh cmd 'bash scripts/reference/eslintConfigSnapshot.sh --compare .crabbox/out/eslint-baseline .crabbox/out/eslint-baseline'` → rc=0, leeres Delta
- **Negativ-Selbsttest (Pflicht — ohne ihn ist der Vergleicher nicht abgenommen):** `iter.sh cmd 'rm -rf /tmp/es-neg && cp -r .crabbox/out/eslint-baseline /tmp/es-neg && echo "{}" > /tmp/es-neg/frontend.json && ! bash scripts/reference/eslintConfigSnapshot.sh --compare .crabbox/out/eslint-baseline /tmp/es-neg'` — der Vergleich **muss rc=1** liefern (das führende `!` dreht ihn für den Verify auf grün). Ein Vergleicher, der hier rc=0 meldet, macht die Abnahmekriterien von T19 und T20 wertlos.
- lokal: `c=$(grep -c '"@typescript-eslint/' docs/analysis/eslint-config-baseline-v8.md); echo "ts-regeln=$c"; [ "$c" -gt 30 ]`
i18n: keine
Doku: `docs/analysis/eslint-config-baseline-v8.md`
Abhängt von: —

### T19 — Flat Config einführen (noch unter ESLint 8)  [ ]
Komponente: apps/frontend, apps/api, libs (Tooling) · Dateien: `eslint.config.mjs` (neu), `apps/frontend/eslint.config.mjs` (neu), `apps/api/eslint.config.mjs` (neu), `libs/ui-kit/eslint.config.mjs` (neu), `package.json` (devDeps)
Soll: Kein Bundle-Anker (Toolchain, fork-original). Tragende Randbedingung: `eslint-config-airbnb@19` und `eslint-config-airbnb-typescript@18` sind **nicht** flat-nativ und müssen über `FlatCompat` aus `@eslint/eslintrc` eingebunden werden.
Änderung: Zwischenschritt, damit Flat-Config und Major-Bump getrennt verifizierbar sind — ESLint 8.57 versteht Flat Config bereits mit `ESLINT_USE_FLAT_CONFIG=true`.
- devDeps ergänzen: `@eslint/js@^9.39.5`, `@eslint/eslintrc@^3.3.6`. `eslint` bleibt in dieser Task auf `^8.57.0`, `typescript-eslint`/`@typescript-eslint/*` bleiben auf `^7.14.1`.
- Root-`eslint.config.mjs` (SPDX-`//`-Header): `FlatCompat` mit `baseDirectory: import.meta.dirname` und `recommendedConfig: js.configs.recommended`; über `...compat.extends('airbnb','airbnb-typescript','plugin:react/recommended','plugin:jsx-a11y/recommended','plugin:@typescript-eslint/recommended','plugin:@typescript-eslint/recommended-requiring-type-checking','prettier','plugin:import/recommended')` genau die acht Einträge des heutigen `extends` in **derselben Reihenfolge** einziehen (die Reihenfolge entscheidet, welche Regel gewinnt — `prettier` muss vor `plugin:import/recommended` bleiben, wie heute). `env` → `languageOptions.globals` (`globals.node`, `globals.browser`, `globals.es2022`, `globals.jest`; `globals` als devDep ergänzen). `parserOptions.project: ['./tsconfig.base.json']` nach `languageOptions.parserOptions`. **Zwingend mitportieren — das fehlte bisher und ist der eigentliche Blocker:** (1) `settings` aus `.eslintrc.json:26-41` — `import/parsers` = `{ '@typescript-eslint/parser': ['.ts', '.tsx'] }` **und** `import/resolver` = `{ typescript: { alwaysTryTypes: true, project: [...] } }` mit **allen vier** Pfaden (`apps/frontend/tsconfig.json`, `apps/api/tsconfig.json`, `libs/tsconfig.json`, `libs/ui-kit/tsconfig.json`), sonst feuert `import/no-unresolved` auf jeden `@libs/*`-Import; (2) das explizite `plugins`-Array mit **fünf** Einträgen (`react`, `@typescript-eslint`, `import`, `prettier`, `jsx-a11y`, `.eslintrc.json:25`) als Flat-Plugin-Objekt — `FlatCompat` zieht nur die Plugins der `extends`-Configs mit, `prettier` steht ausschließlich im expliziten Array. `ignorePatterns` → `{ ignores: [...] }` als **erstes** Config-Objekt, inhaltlich 1:1 die heutigen **9** Einträge (nachgezählt, `.eslintrc.json:42-52`). Die **8** `rules`-Einträge (nachgezählt, `.eslintrc.json:53-82`) und die drei `overrides` (`index.ts` → `import/prefer-default-export: off`; `libs/src/**` → `project: libs/tsconfig.json`; `libs/ui-kit/**` → `project: libs/ui-kit/tsconfig.json`) als eigene Config-Objekte mit `files`-Filter nachbilden.
- Drei Projekt-Configs, die die Root-Config importieren und ihre Zusätze anhängen: Frontend (Browser-Globals, `project: apps/frontend/tsconfig.eslint.json`, ignores `jest.config.ts`/`vite.config.mts`/`postcss.config.mjs`/`tailwind.config.ts`/`public/*`), API (Node-Globals, die fünf API-Regeln, `project: apps/api/tsconfig.eslint.json`, `*.spec.ts`-Override mit `no-explicit-any`/`unbound-method` off, ignores `jest.config.ts`/`webpack.config.js`/`vite.config.mts`), ui-kit (`react/react-in-jsx-scope: off`, `project: ./tsconfig.json`, ignores der drei Config-Dateien).
- Die vier `.eslintrc.json` bleiben in dieser Task **liegen** (Rückfallebene); entfernt werden sie erst in T20.
Verify (**das Abnahmekriterium umfasst alle vier Blöcke, nicht nur `rules`**; das Ausgabe-Verzeichnis ist **Pflichtargument** — schreibt der Flat-Lauf in die Baseline, vergleicht `--compare` ein Verzeichnis mit sich selbst und ist unbedingt grün):
- `iter.sh cmd 'ESLINT_USE_FLAT_CONFIG=true bash scripts/reference/eslintConfigSnapshot.sh .crabbox/out/eslint-flat'`
- `iter.sh cmd 'test -d .crabbox/out/eslint-baseline && test -d .crabbox/out/eslint-flat && bash scripts/reference/eslintConfigSnapshot.sh --compare .crabbox/out/eslint-baseline .crabbox/out/eslint-flat; echo "[rc=$?]"'` → `[rc=0]` und **leeres Delta über `rules` + `settings` + `plugins` + `parserOptions.project`**. Beide Verzeichnisse müssen existieren; fehlt eines, bricht die Kette vor dem Vergleich ab und `[rc=1]` macht das sichtbar. (**Kein `|| true` anhängen** — das schluckt genau den Fehlschlag, den dieses Gate fangen soll.)
- Direkte Gegenprobe auf den Resolver: `iter.sh cmd 'ESLINT_USE_FLAT_CONFIG=true npx eslint --print-config apps/frontend/src/App.tsx | grep -c "alwaysTryTypes"'` > 0
- `iter.sh cmd 'ESLINT_USE_FLAT_CONFIG=true npm run lint'` mit **derselben** Fehleranzahl wie `iter.sh lint` ohne die Variable und **ohne** `import/no-unresolved`-Treffer auf `@libs/*`
i18n: keine
Doku: Abschnitt „Flat Config" in `docs/features/p7-fe-toolchain-catchup.md` (warum FlatCompat für airbnb, warum Zwischenschritt unter ESLint 8)
Abhängt von: T18

### T20 — ESLint 9 + typescript-eslint 8; .eslintrc entfernen  [ ]
Komponente: apps/frontend, apps/api, libs (Tooling) · Dateien: `package.json`, `package-lock.json`, `eslint.config.mjs`, `.eslintrc.json` (löschen), `apps/frontend/.eslintrc.json` (löschen), `apps/api/.eslintrc.json` (löschen), `libs/ui-kit/.eslintrc.json` (löschen)
Soll: Kein Bundle-Anker (Toolchain). Gemessene Randbedingungen: `typescript-eslint@8.65.0` deklariert `peerDependencies.eslint = '^8.57.0 || ^9.0.0 || ^10.0.0'`; `eslint@9` aktuell = `9.39.5`.
Änderung: `eslint` → `^9.39.5`, `typescript-eslint`/`@typescript-eslint/parser`/`@typescript-eslint/eslint-plugin` → `^8.65.0`, `eslint-plugin-react` → `^7.37.5`, `eslint-plugin-jsx-a11y` → `^6.10.2`, `eslint-plugin-import` → `^2.32.0`, `eslint-config-prettier` → `^10.1.8`, `eslint-plugin-prettier` → `^5.5.6`, `@eslint/js` → `^9.39.5`. Die vier `.eslintrc.json` löschen, `ESLINT_USE_FLAT_CONFIG` nirgends mehr setzen (ESLint 9 nimmt Flat Config von selbst).
Zwei bekannte, konkrete Bruchstellen in `eslint.config.mjs` beheben:
1. Der Config-Name `plugin:@typescript-eslint/recommended-requiring-type-checking` existiert in v8 nicht mehr → auf `plugin:@typescript-eslint/recommended-type-checked` umstellen.
2. `eslint-config-airbnb-typescript@18` aktiviert genau **drei** Regeln, die typescript-eslint v8 entfernt/umbenannt hat und die `eslint-config-prettier` **nicht** abdeckt (die 14 Formatier-Pendants `brace-style`, `comma-dangle`, `comma-spacing`, `func-call-spacing`, `indent`, `keyword-spacing`, `no-extra-parens`, `no-extra-semi`, `object-curly-spacing`, `quotes`, `semi`, `space-before-blocks`, `space-before-function-paren`, `space-infix-ops` schaltet prettier bereits auf `off`, deshalb schlagen sie **nicht** als „Definition for rule not found" durch). Zu behandeln sind daher nur: `@typescript-eslint/lines-between-class-members` → `off` + Basis-Regel `lines-between-class-members` mit der bisherigen Airbnb-Konfiguration reaktivieren; `@typescript-eslint/no-loss-of-precision` → `off` + Basis-Regel `no-loss-of-precision: 'error'`; `@typescript-eslint/no-throw-literal` → `off` + `@typescript-eslint/only-throw-error` mit derselben Severity. Diese drei Anpassungen als **eigenes, kommentiertes Config-Objekt am Ende** der Root-Config, damit sie beim nächsten Upgrade auffindbar sind.
Verify:
- `iter.sh cmd 'npm ci && npx eslint --version'` → beginnt mit `9.`
- `iter.sh cmd 'bash scripts/reference/eslintConfigSnapshot.sh .crabbox/out/eslint-v9 && bash scripts/reference/eslintConfigSnapshot.sh --compare .crabbox/out/eslint-baseline .crabbox/out/eslint-v9'` — **eigenes Ausgabe-Verzeichnis ist Pflicht**, sonst überschreibt der Lauf die T18-Baseline und der Vergleich wird unbedingt grün. Erwartung: Delta enthält **ausschließlich** die drei oben genannten Regel-Umbenennungen; `settings`, `plugins` und `parserOptions.project` **unverändert**.
- `iter.sh lint` grün mit derselben Fehleranzahl wie in T18/T19 und **ohne** „Definition for rule … was not found"
- Löschprüfung locale-unabhängig (**kein** `grep "No such file"` — die Box antwortet „Datei oder Verzeichnis nicht gefunden"): `iter.sh cmd 'c=0; for f in .eslintrc.json apps/frontend/.eslintrc.json apps/api/.eslintrc.json libs/ui-kit/.eslintrc.json; do [ -f "$f" ] || c=$((c+1)); done; echo "geloescht=$c"; [ "$c" = 4 ]'` → `geloescht=4`
i18n: keine
Doku: Abschnitt „ESLint 9" in `docs/features/p7-fe-toolchain-catchup.md` — die drei neutralisierten Regeln mit Begründung, der `recommended-type-checked`-Rename, und die Rückfall-Option `eslint-config-airbnb-extended`, falls die FlatCompat-Brücke beim nächsten Airbnb-Bump reißt
Abhängt von: T19

### T21 — Lint-Verdrahtung nachziehen: nx, lint-staged, pre-commit, CI  [ ]
Komponente: apps/frontend, apps/api, libs (Tooling) · Dateien: `libs/project.json` (Z. 8-15), `apps/frontend/project.json` (Z. 8-14), `apps/api/project.json` (Z. 8-14), `lint-staged.config.js`, `.husky/pre-commit`, `.github/workflows/*` (nur der Lint-Step), `AGENTS.md`
Soll: Kein Bundle-Anker (Toolchain). Bestand: alle drei Lint-Targets nutzen `@nx/eslint:lint`; `libs/project.json` setzt zusätzlich `lintFilePatterns: ['libs/**/*.{ts,tsx}']` und `tsConfig: ['libs/tsconfig.json']`; `lint-staged.config.js` ruft `npx eslint --fix <files>`; `.husky/pre-commit` fährt 11 Checks, davon `npm run lint:staged` als letzten.
Änderung: Nach dem Wegfall der `.eslintrc.json` prüfen, ob `@nx/eslint@22.3.0` die Flat Configs je Projekt korrekt auflöst; falls `lintFilePatterns` unter Flat Config nicht mehr greift, in `libs/project.json` auf die von nx 22 unterstützte Form umstellen und die `tsConfig`-Option nur entfernen, wenn sie nachweislich wirkungslos ist (sonst stehen lassen — keine Drive-by-Änderung). `lint-staged.config.js` und `.husky/pre-commit` inhaltlich unverändert lassen, sofern `npx eslint --fix` unter ESLint 9 sauber läuft; nur falls nötig anpassen. **CI: nichts zu ändern — belegt, nicht vermutet.** Es gibt genau zwei Lint-Aufrufe: `.github/workflows/build-and-test.yml:208` (`npm run lint`) und `.github/workflows/container-build.yml:97` (`npm run lint && npm run test && npx nx test frontend && npm run check-translations`). **Keiner** benutzt `--ext` (das es in Flat Config nicht gibt). Beide bleiben unangetastet; diese Zeile steht hier, damit die Workflows nicht „vorsichtshalber" umgebaut werden. In `AGENTS.md` den Abschnitt „Linting" von „ESLint (Airbnb + TypeScript …)" auf ESLint 9 / Flat Config aktualisieren und den Pfad `eslint.config.mjs` nennen.
Verify: `iter.sh lint` grün (`nx run-many --target=lint --projects=frontend,api,libs`); `iter.sh cmd 'npx nx run libs:lint'` lintet nachweislich Dateien (Ausgabe nennt eine Dateizahl > 0, nicht „no files matched"); `iter.sh cmd 'npx lint-staged --help'` ohne Fehler; `iter.sh cmd "git diff --name-only HEAD~1 -- '*.ts' '*.tsx' | head -1 | xargs -r npx eslint"` läuft ohne Config-Fehler; `iter.sh cmd "grep -q 'eslint.config.mjs' AGENTS.md && echo OK"` → `OK`.
i18n: keine
Doku: `AGENTS.md`-Abschnitt „Coding Style & Naming Conventions" (Linting-Zeile)
Abhängt von: T20

### T22 — Visual-Baselines: 2.0.156 als überholt markieren, Verfahren gegen 2.1.0 festschreiben  [ ]
Komponente: apps/frontend (Doku/Tooling) · Dateien: `docs/testing/visual-baselines.md` (neu), `scripts/crabbox/shots.py` (Modul-Liste)
Soll: Kein Bundle-Anker (Verfahren). Bestand: `.reference/2.0.156/baselines/` enthält 11 PNGs + `report.json`, aufgenommen gegen echten LMN (global-admin, Playwright/chromium, Viewport 1600×1000); die `PROVENANCE.txt` dort hält fest, dass 2.0.200 **nicht mehr beschaffbar** ist (`:latest` ist seit 2026-07-17 = 2.1.0) und 2.0.156 nur der höchste verfügbare 2.0-Tag war. `.reference/` ist gitignored → nichts darin ist committbar.
Änderung: Neues Dokument `docs/testing/visual-baselines.md` (DE, mit EN-Kurzabschnitt analog `docs/observability.md`), das festhält: (1) **`.reference/2.0.156/baselines/*` ist ab jetzt der falsche Maßstab** — das Fork-Ziel ist 2.1.0; die alten Shots bleiben nur als historische 2.0-Referenz liegen; (2) Beschaffungsweg der 2.1.0-Referenz (`REF_VERSION=2.1.0 REF_TAG=latest bash scripts/crabbox/fetch-reference.sh`, nach T1 der Default) inkl. der Provenance-Zeile `2.1.0 (rev 5c58d5818c1efff0422e914bf5785209b972cec6, main.js=94907 Zeilen)`; (3) Aufnahmeverfahren (`iter.sh deploy` → `iter.sh shots`, Viewport 1600×1000, Erfolgskriterium aus `shots.py`: nach dem Login `/dashboard` **und** `GET /edu-api/lmn-api/auth` = 200); (4) Ablageort `.reference/2.1.0/baselines/` + committbares Manifest (Modul, Pfad, Screenshot-Name, Aufnahmedatum, Image-Revision) im Dokument selbst, weil `.reference/` gitignored ist; (5) welche Module aufgenommen werden.
In `shots.py` die `DEFAULT_MODULES`-Liste um die in 2.1.0 vorhandenen, im Fork aber noch nicht gebauten Module **nicht** blind erweitern — stattdessen dokumentieren, dass sie per `EDU_MODULES="name:/pfad,…"` zugeschaltet werden, und nur solche Module in die Default-Liste aufnehmen, die im Fork tatsächlich geroutet sind. Die neun bestehenden Einträge bleiben unverändert.
Verify: `iter.sh cmd 'test -f docs/testing/visual-baselines.md && grep -q "2.0.156" docs/testing/visual-baselines.md && grep -q "94907" docs/testing/visual-baselines.md && grep -q "EDU_MODULES" docs/testing/visual-baselines.md && echo OK'` → `OK`; `iter.sh cmd 'python3 -c "import ast,sys; ast.parse(open(\"scripts/crabbox/shots.py\").read())" && echo OK'` → `OK`.
i18n: keine
Doku: dieses Dokument; Querverweis aus `docs/testing/spec-policy.md` und aus dem `/test`-Skill-Kontext
Abhängt von: —

### T23 — Baselines gegen 2.1.0 neu aufnehmen  [?] human-gate: braucht warme Box + Voll-Stack-Deploy gegen echten LMN + Kevins Freigabe
Komponente: apps/frontend (Verifikation) · Dateien: `.reference/2.1.0/baselines/*` (gitignored, nicht committbar), Manifest-Abschnitt in `docs/testing/visual-baselines.md`
Soll: Referenz-Instanz = `ghcr.io/edulution-io/edulution-{api,ui}:latest` = 2.1.0 (rev `5c58d5818c1efff0422e914bf5785209b972cec6`). Die alten Shots unter `.reference/2.0.156/baselines/` sind der falsche Maßstab und werden **nicht** überschrieben.
Änderung: Kein Code. Voll-Stack der **2.1.0-Referenz** auf der crabbox gegen den echten LMN hochziehen, mit `shots.py` (Viewport 1600×1000, global-admin) die Modul-Screenshots aufnehmen und nach `.reference/2.1.0/baselines/` legen; `report.json` mitschreiben. Anschließend das Manifest in `docs/testing/visual-baselines.md` mit Aufnahmedatum, Image-Revision, Modulliste und dem Login-Erfolgskriterium füllen (nur das Manifest ist committbar). Nach außen wirkende Schritte (`iter.sh deploy` gegen den echten LMN) sind prompt-pflichtig — Task auf `[?]` lassen, bis Kevin freigibt, und **weiterlaufen**.
Verify: `iter.sh deploy` grün; `iter.sh shots` liefert `login.on_dashboard === true`, `login.still_on_login === false` und `lmn_api` = 200; für jedes Modul `rendered === true` und `kicked_to_login === false`; `.reference/2.1.0/baselines/` enthält mindestens so viele PNGs wie `.reference/2.0.156/baselines/` (11) plus `report.json`; das Manifest im Doku-Dokument listet exakt die aufgenommenen Dateien.
i18n: keine
Doku: Manifest-Abschnitt in `docs/testing/visual-baselines.md`
Abhängt von: T22 · (empfohlen nach T17, damit derselbe Lauf den Fork-Build gegen die frische Referenz stellen kann)
